name: Tower Breaker observable runtime test

on:
  pull_request:
    branches: [main]
    paths:
      - 'towerbreaker-trigger/**'

permissions:
  contents: read

jobs:
  build-test:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Reconstruct corrected game source
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p /tmp/tb/parts /tmp/tb/project /tmp/tb/runtime
          for n in 00 01 02 03 04; do
            curl -fsSL "https://raw.githubusercontent.com/privacyOG/kivy-python-for-android/tower-breaker-fix-ci/towerbreaker-ci/parts/part${n}" -o "/tmp/tb/parts/part${n}"
          done
          cat /tmp/tb/parts/part* | tr -d '\n\r' | base64 --decode > /tmp/tb/project.zip
          unzip -tq /tmp/tb/project.zip
          unzip -q /tmp/tb/project.zip -d /tmp/tb/project

      - name: Apply frame attachment safeguards
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('/tmp/tb/project/app/src/main/java/com/privacyog/towerbreaker/GameView.java')
          s = p.read_text()
          s = s.replace(
              "        if (running) return;\n        running = true;\n        previousNanos = System.nanoTime();\n        removeCallbacks(frameRunnable);\n        post(frameRunnable);\n        invalidate();",
              "        if (running) return;\n        if (!isAttachedToWindow()) {\n            running = false;\n            return;\n        }\n        running = true;\n        previousNanos = System.nanoTime();\n        removeCallbacks(frameRunnable);\n        postOnAnimation(frameRunnable);\n        invalidate();"
          )
          s = s.replace(
              "            if (!running || !isAttachedToWindow()) return;",
              "            if (!running) return;\n            if (!isAttachedToWindow()) {\n                running = false;\n                return;\n            }"
          )
          for token in ('if (!isAttachedToWindow())', 'postOnAnimation(frameRunnable)', 'postOnAnimation(this)', 'protected void onAttachedToWindow()'):
              if token not in s:
                  raise SystemExit('Missing safeguard: ' + token)
          p.write_text(s)
          PY

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - name: Install Android API 36 components
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager 'platforms;android-36' 'build-tools;36.0.0' 'platform-tools'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Build APK
        working-directory: /tmp/tb/project
        shell: bash
        run: |
          set -o pipefail
          gradle --no-daemon --stacktrace clean assembleDebug 2>&1 | tee /tmp/tb/runtime/build.log
          cp app/build/outputs/apk/debug/app-debug.apk /tmp/tb/runtime/TowerBreaker-1.0.1-fixed.apk

      - name: Emulator launch and black-screen test
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          arch: x86_64
          profile: pixel_6
          target: google_apis
          disable-animations: true
          emulator-options: -no-window -gpu swiftshader_indirect -no-snapshot -noaudio -no-boot-anim -camera-back none
          script: |
            set -eu
            adb install -r /tmp/tb/runtime/TowerBreaker-1.0.1-fixed.apk
            adb logcat -c
            adb shell am force-stop com.privacyog.towerbreaker.debug
            adb shell monkey -p com.privacyog.towerbreaker.debug -c android.intent.category.LAUNCHER 1
            sleep 7
            adb shell pidof com.privacyog.towerbreaker.debug
            adb exec-out screencap -p > /tmp/tb/runtime/title.png
            python3 /tmp/tb/project/scripts/check_screenshot.py /tmp/tb/runtime/title.png
            DIMS=$(python3 -c "import struct; d=open('/tmp/tb/runtime/title.png','rb').read(24); print(*struct.unpack('>II', d[16:24]))")
            set -- $DIMS
            adb shell input tap $(( $1 / 2 )) $(( $2 * 65 / 100 ))
            sleep 4
            adb shell pidof com.privacyog.towerbreaker.debug
            adb exec-out screencap -p > /tmp/tb/runtime/gameplay.png
            python3 /tmp/tb/project/scripts/check_screenshot.py /tmp/tb/runtime/gameplay.png
            python3 - <<'PY'
            import sys
            sys.path.insert(0, '/tmp/tb/project/scripts')
            from check_screenshot import read_png
            from pathlib import Path
            w1, h1, c1, a = read_png(Path('/tmp/tb/runtime/title.png'))
            w2, h2, c2, b = read_png(Path('/tmp/tb/runtime/gameplay.png'))
            if (w1, h1, c1) != (w2, h2, c2):
                raise SystemExit('Screenshot formats differ')
            step_x = max(1, w1 // 180)
            step_y = max(1, h1 // 100)
            changed = total = diff_sum = 0
            stride = w1 * c1
            for y in range(0, h1, step_y):
                row = y * stride
                for x in range(0, w1, step_x):
                    i = row + x * c1
                    d = abs(a[i]-b[i]) + abs(a[i+1]-b[i+1]) + abs(a[i+2]-b[i+2])
                    diff_sum += d
                    changed += d > 36
                    total += 1
            ratio = changed / max(1, total)
            mean = diff_sum / max(1, total * 3)
            print(f'title-to-gameplay change: ratio={ratio:.3f}, mean-channel-delta={mean:.2f}')
            if ratio < 0.08 or mean < 8.0:
                raise SystemExit('Start interaction did not produce a distinct gameplay screen')
            PY
            adb logcat -d > /tmp/tb/runtime/logcat.txt
            ! grep -E 'FATAL EXCEPTION|AndroidRuntime.*Process: com\.privacyog\.towerbreaker\.debug' /tmp/tb/runtime/logcat.txt

      - name: Record verification
        if: success()
        shell: bash
        run: |
          APK=/tmp/tb/runtime/TowerBreaker-1.0.1-fixed.apk
          {
            echo 'RESULT=PASS'
            echo 'RUNTIME=Android 15 API 35 x86_64 Pixel 6 emulator'
            echo "SHA256=$(sha256sum "$APK" | awk '{print $1}')"
            echo "BYTES=$(stat -c '%s' "$APK")"
            echo 'TITLE_SCREEN=NON_BLACK'
            echo 'GAMEPLAY_SCREEN=NON_BLACK_AND_DISTINCT'
            echo 'PROCESS_ALIVE=YES'
            echo 'FATAL_LOGCAT=NONE'
          } > /tmp/tb/runtime/verification.txt

      - name: Upload APK and evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: TowerBreaker-1.0.1-fixed
          path: /tmp/tb/runtime/
          if-no-files-found: error
          retention-days: 14
